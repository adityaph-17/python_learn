# A function is a group of statements performing a specific task.

def avg():
    a = int(input('Enter first num :'))
    b = int(input('Enter second num :'))
    c = int(input('Enter third num :'))

    avg = (a+b+c)/3
    print (avg)
    print("\n")

avg() # function call :- Whenever we want to call a function, we put the name of the function followed by parentheses as follows:
avg()
avg()
